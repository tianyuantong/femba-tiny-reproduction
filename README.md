# FEMBA-Tiny 2025 复现与精度实验

基于 FEMBA 2025 年公开代码和预训练权重，在 TUAR 上复现二分类基线，并比较低精度推理的质量变化。

> **权重量化到 8 位无损；激活一旦量化到 8 位，模型就把全部样本判为同一类。**
> 原因是激活值里极少数超大值撑满了量化量程。Hadamard 旋转把峰值压小 4.3–11.1 倍，
> 仍未恢复。这组 PTQ 结果作为后续 QAT 阶段的输入。

单种子 seed 42 · 7,090 个测试窗口 · RTX 5060 Ti · 量化采用 FP32 QDQ 模拟。

## FP32 复现

![FP32 复现结果与论文 Table IV 的差距](reproduction/figures/fp32-gap.svg)

| FP32 轨迹 | 标签／AdamW 学习率 | AUROC | AP | 角色 |
| --- | --- | ---: | ---: | --- |
| 公开代码配方 | 18 类、占比 ≥30%／5e-4 | **0.923889** | 0.933093 | **复现结果** |
| 论文标签对照 | 13 类、任一出现／5e-4 | 0.913040 | 0.944898 | 保留代码训练配方，检验标签变更 |
| 学习率对照 | 13 类、任一出现／1e-4 | 0.898821 | 0.938437 | 按验证 loss 选定的**精度母模型** |
| 2025 论文 Table IV | TUAR 二分类 | 0.937 ± 0.008 | 0.912 ± 0.010 | 参考值 |

**0.923889 是复现结果**，距论文中心 0.013，约 1.6 个标准差。下面两条各自只改一项设置
（标签协议、学习率），是有意为之的对照，不是复现退步。

论文标签组保留代码的归一化、AdamW 和模型实现。两组学习率的最低验证 loss 分别为
0.426979、0.393690；按预先确定的最低验证 loss 规则选出 1e-4 组作为精度母模型 ——
它的验证 loss 更低但测试 AUROC 更低，两种指标的最优点并不一致。学习率对照到此结束。

## 精度阶梯

![各精度版本的 AUROC，权重量化保持基线而激活量化崩到随机水平](reproduction/figures/precision-ladder.svg)

下表均使用 AUROC 0.898821 的母模型；A8 对比 57 处位置与 22 个 Linear 输入边界。

| 版本 | AUROC | AP | 结果 |
| --- | ---: | ---: | --- |
| FP32 | 0.898821 | 0.938437 | 当前母模型 |
| W8A32 | 0.899945 | 0.938976 | 在预设精度容差内 |
| W4A32 | 0.844892 | 0.903830 | 明显退化 |
| W8A8，57 处／浮点尺度 | 0.500000 | 0.577433 | 全部预测为正类 |
| W4A8，57 处／浮点尺度 | 0.500000 | 0.577433 | 全部预测为正类 |
| W8A8，57 处／二次幂尺度 | 0.500000 | 0.577433 | 全部预测为正类 |
| FP16：直接转换／状态保留／AMP | — | — | 首批验证出现 NaN |
| Rot-W8A32 | 0.900130 | 0.939189 | 在预设精度容差内 |
| Rot-W4A32 | 0.821896 | 0.890039 | 低于未旋转 W4A32 |
| Rot-W8A8，57 处 | 0.500000 | 0.577433 | 全部预测为正类 |
| Rot-W4A8，57 处 | 0.500000 | 0.577433 | 全部预测为正类 |
| W8A8，22 个 Linear 输入 | 0.526839 | 0.604017 | 全部预测为正类 |
| W4A8，22 个 Linear 输入 | 0.546794 | 0.619750 | 全部预测为正类 |

### A8 为什么崩

静态 max 校准按这批数里的最大值定刻度。模型某处输入里极少数元素比其余大两个数量级
以上，把刻度撑得极宽，其余元素四舍五入后全部归零：

> 验证与测试合计 **16.0 亿**个元素，其中 **99.9964%** 从非零被量化成零，
> 仅 5 个元素发生裁剪。主要损失不是裁剪，是归零。

一层输出近乎全零，后面自然判断不出任何东西。这是 Mamba 类模型已知的激活离群值现象。

Hadamard 旋转正是针对这一点：它保持向量范数不变，把单个离群值摊到一个 128 维块上，
从而压低峰值。本轮实测把五处输出投影的输入峰值压小了 **4.3–11.1 倍**，A8 分类仍未恢复，
说明仅靠旋转不足以救回静态 PTQ。

收窄量化范围（57 处 → 22 个 Linear 输入边界）只把 AUROC 从 0.500 抬到 0.527／0.547，
仍然全部预测正类 —— **范围不是主因，校准才是。**

旋转通过 [2026-09-22 修订协议](reproduction/PROTOCOL.md#2026-09-22旋转数值检查修订)的一致性检查。

### 这是预期结果，不是缺陷

PTQ 在低位宽崩溃是已知现象：2025 论文本身不报 PTQ 数字，而同组 2026 年那篇在 TUAB 上
报的 W8A8 PTQ 只有 AUROC 0.77、Accuracy 55.91%（[来源与对照](reproduction/RESULTS.md)）。
本阶段的交付是**测出崩溃并定位原因**，恢复精度属于后续 QAT 阶段。

## 详细结果

[完整结果与关键对照](reproduction/RESULTS.md) · [复现步骤与实验口径](reproduction/PROTOCOL.md) ·
[精度结果 JSON](reproduction/precision_results.json) · [Linear 边界对照](reproduction/linear_scope_results.json) ·
[旋转结果](reproduction/rotation_results.json)

## 来源

基于 [pulp-bio/BioFoundation 的 2025 快照](https://github.com/pulp-bio/BioFoundation/tree/d88596590f3bd3fce573be07646b7d3977ce7bcc)
和 [PulpBio/FEMBA 预训练权重](https://huggingface.co/PulpBio/FEMBA)，论文见
[FEMBA（2025）](https://arxiv.org/abs/2502.06438v2)。上游代码采用 [Apache-2.0](LICENSE)。
仓库提供代码和结果摘要；数据集、权重及逐样本预测保留在本地。
